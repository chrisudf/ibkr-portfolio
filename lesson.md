# Lessons

踩过的坑，按主题分组。每条记：**现象 → 原因 → 修法 → 教训**。

写在这里的标准是「下次还会再踩」，不是「这次修好了」。

---

## IBKR 数据本身

### 1. 利息的预扣税会伪装成分红的预扣税

**现象**：U22 的分红预扣税比手算多 $9.15，净分红少同样金额，`by_symbol`
里还多出一个标的为「—」的行。

**原因**：IBKR 把**信用利息**的预扣税也归在 `Withholding Tax` 类型下（
Statement of Funds 里则是同一个 `FRTAX` 代码）：

```
Type=Withholding Tax  Symbol=(空)  Amount=-9.15
Description=WITHHOLDING @ 10% ON CREDIT INT FOR NOV-2025
```

按类型/代码筛选，就会把它算进分红。

**修法**：真实的分红预扣税 84 行**全部带 Symbol**，这一行没有。所以规则是
「归不到标的的预扣税不算分红预扣税」。`DividendType` 列指望不上 —— 所有
Withholding Tax 行上它都是空的。

**教训**：IBKR 的 `Type` / `ActivityCode` 描述的是**现金动作**，不是业务含义。
同一个类型下混着不同来源的钱。判定业务归属要另找锚点（这里是「有没有标的」）。

### 2. NAV section 是日频序列，不是一行

**现象**：无。这是个**潜在**问题，被真实报表暴露出来的。

**原因**：`_ingest_nav` 当初按「一个账户一行」写的，靠后来居上。而 365 天的
query 每账户吐 **262 行**（一天一行）。结果正确纯属 IBKR 恰好按日期升序发。

**修法**：改成比 `ReportDate` 取最新，并用打乱行序的同一份报表验证结果不变。

**教训**：这类 bug 最坏 —— 一旦 IBKR 改了发送顺序，面板会**安静地**显示一年前
的 NAV，而页面上每个百分比都跟着错，没有任何报错。凡是「靠遍历顺序取最后一个」
的地方，都该问一句「顺序是谁保证的」。

### 3. 列名是 `Date/Time` 不是 `DateTime`

**现象**：给 Cash Transactions 写的日期列降级分支，对着真实输出是死代码。

**原因**：真实 Flex 输出用 `Date/Time`（带斜杠），值形如 `20260311;120000`。
`SettleDate` 在的时候用不着它，所以没暴露。

**教训**：列名别凭印象写。没有真实样本时，多写几个候选拼写比赌一个强。

### 4. 代付股息（PIL）不报每股费率

**现象**：ETHU 13 笔派息里有 3 笔算不出每股股息。

**原因**：普通派息的描述带费率（`USD 0.346643 PER SHARE`），但
`Payment In Lieu Of Dividends`（股票被借出时产生）只有一句
`PAYMENT IN LIEU OF DIVIDEND`，没有费率。

**修法**：计入税前总额但不计入每股合计，UI 打 `*` 号说明「实际略高于显示值」。

**教训**：同一个数据源里，不同子类型的字段完整度不一样。缺失要显式标出来，
不要让它悄悄拉低一个比率。

---

## 计算口径

### 5. 「总分红 ÷ 当前成本」在仓位变动时会失真到离谱

**现象**：U17 的 METU 成本股息率算出 **36.82%**。

**原因**：分子是一整年的分红（当时隐含持股 137 股），分母是今天剩下的
11.85 股的成本 —— 两边根本不是同一个东西。反过来，分红后加仓的标的
（GOOG）会被**低估**。

**修法**：改成「本期实收**每股**股息 ÷ **每股**平均成本」，两边都是每股，
仓位变化自动抵消。METU 从 36.82% 变成 3.19%，差 33 个百分点。

**教训**：分红是**期间流量**，成本是**时点存量**。两者相除前，先确认分子分母
描述的是同一批股份。

### 6. 重建成本只对「已清仓」有效，对「仍持有」是错的

**现象**：想用 Statement of Funds 的买入记录重建成本价，对仍持有的标的验证时
NVDL 重建 $80.87 vs IBKR 真实 $31.23（差 **159%**），CONL 差 58%。

**原因**：期间做过来回的标的，买入 tally 里混着已经卖掉的股份。IBKR 的
成本价只算**剩余批次**，简单平均算的是**所有经手股份**。

**修法**：只在没有当前持仓时才用重建值（此时买的每一股都卖掉了，两者等价）；
有 IBKR 的 `CostBasisPrice` 就用它。另加 `covered` 守卫挡住「建仓早于报表期间」
的情况 —— 买入量盖不住卖出量说明前半段不在数据里。

**验证方法值得记**：拿 IBKR 自己的已实现盈亏反推 ——
`买入成本 + 佣金` 应该等于 `卖出总收入 − TotalRealizedPnl`。14 个已清仓标的
全部 0.00% 偏差。**用数据源里的冗余字段互相验证，比自己写断言可信得多。**

### 7. 每股股息在合并视图里不能求和

**现象**：合并两个账户时，MSFT 每股股息会变成 $3.64。

**原因**：每股股息是**证券属性**不是账户属性 —— 两个账户各自持有 MSFT，
收到的都是每股 $1.82。

**修法**：取各账户的最大值（持有期最长的那个覆盖最全），不是求和。成本价则
按 tally 求和后重算加权均价，而不是对均价再平均。

**教训**：合并多账户时，逐字段问「这个量是可加的吗」。金额可加，**比率和
单价不可加**。

### 8. 保证金估算里，「按张数报」会掩盖不确定性

**现象**：面板写「16 张合约按平值估算」，看着像零头。

**原因**：那 16 张占了估算额的 **68%**。平值假设是最贵的猜法，所以缺报价的
合约往往正好是要求最大的那些。

**修法**：改成按**金额**报占比。

**教训**：报告估算质量时，用「占结果的比重」而不是「占条目数的比重」。

---

## 运维 / 工程

### 9. token 会顺着异常信息漏进日志和 API 响应

**现象**：网络异常时 `str(exc)` 带出完整 URL，而 token 是 URL 的 query 参数：

```
network error on SendRequest: connection refused for
https://...FlexStatementService.SendRequest?t=<真实token>&q=1549141&v=3
```

IBKR 的错误信封里也会回显带 token 的 `<Url>`。

**修法**：`redact()` 对 token 明文和 `t=` 参数各扫一遍，日志和 HTTP 响应
都过一道。

**教训**：凭据放在 URL 里的 API，任何「把上下文带进错误信息」的好意都是泄漏面。

### 10. gunicorn 下 `app.logger` 的 INFO 会被静默丢掉

**现象**：本地 `debug=True` 看得到的日志，droplet 上没有。

**原因**：Flask 的 `app.logger` 是 NOTSET，继承 root 的 WARNING。

**修法**：显式 `setLevel(logging.INFO)` 并接管 `gunicorn.error` 的 handler。

**教训**：加诊断日志之后，要在**部署形态下**确认它真的打得出来。开发环境能
看见不代表生产能看见 —— 而诊断日志恰恰是给生产用的。

### 11. IBKR 会因为反复失败而升级成锁定

**现象**：连续 9 次 `1001`（临时不可用）之后，IBKR 改吐
`1025 Too many failed attempts. Please review your configuration.`

**原因**：重试本身触发了锁定。`1001` 的字面意思是「稍后再试」，但**没说
最短间隔**，按 3 分钟一次重试了 36 分钟就被拉黑。

**修法（待办）**：`1025` 目前不在 `PERMANENT_CODES` 里（`parser/flex_fetch.py`
和 `scripts/ibkr_sync.sh` 两处都没有），所以 bash 脚本还会对着一个锁定的
endpoint 把 +2h/+4h 重试梯子走完。

**教训**：对第三方 API，「可重试」不等于「可高频重试」。拿不准间隔时，
宁可退到分钟级以上 —— 尤其是这种一把手动操作就能替代的场景。

### 12. 判断「同步是不是坏了」，先确认你在看哪个实例

**现象**：看到本地 `uploads/*.json` 停在 5 周前，判断「同步已经坏了五周」。

**原因**：`deploy/docker-compose.yml` 给 droplet 挂的是独立的 `app_data`
volume。本地文件陈旧只说明**本地**五周没跑过，跟部署实例上的同步状态无关。

**教训**：多部署形态的项目，任何「从文件时间戳推断系统状态」的结论，都要先
问一句「这是哪个实例的文件」。

---

## 排查手法（有效的，值得重复）

- **用数据源自身的冗余字段互相验证**。重建成本 vs `TotalRealizedPnl` 反推，
  14/14 精确一致 —— 比自己写的断言可信得多。
- **写独立脚本重算，别复用被验证的解析器**。分红逐笔对账用的是另写的
  csv.reader 循环，共用代码的话共同的错误会一起消失。
- **打乱输入顺序再跑一遍**，专治「隐式依赖顺序」类 bug（第 2 条就是这么确认的）。
- **拿不到线上数据时，先从已有产物反推**。Statement of Funds 是否启用，是靠
  `cash_span_days` 在两个账户上不同（239 vs 364）推出来的 —— 那个值只可能
  来自带日期的现金流行。后来真实报表确认完全正确。

### 13. 时间加权平均持仓会被「期内做空」拉成负数

**现象**：自己写的断言抓到 U17 的 USAR `avg_shares = -9.07`。

**原因**：不是算错 —— 那个标的 02-03 卖出 10 股、04-22 才买回，中间 78 天
是**净空头**，按时间积分本来就该是负的。

**修法**：积分时对持仓取 `max(pos, 0)`，只累计多头敞口 —— 空头是**付**分红
的，拿净敞口当「本可收多少分红」的基数没有意义。

**教训**：两点。一是**「持仓数量」这个量在有做空的账户里是有符号的**，凡是
拿它当基数/分母的地方都要先想清楚负值意味着什么。二是这个 bug 当时**在 UI
上看不见**（USAR 不派息，进不了分红表；负数走到除息日缺口判断里也过不了金额
门槛，不会误报）—— 是断言抓出来的，不是眼睛。写完计算顺手加一条不变量断言，
比事后靠肉眼复查划算。

### 14. 杠杆 ETF 的「分红」大部分不是分红

**现象**：METU 的成本股息率算出 4.65%，比 META 本尊（0.26%）高一个数量级。

**原因**：两层。第一层是看错标的 —— **METU 不是 META**，是
`DIREX DLY META BUL 2X ETF`（Direxion 2 倍杠杆 ETF）。第二层才是关键：
杠杆 ETF 持有的是掉期 + 现金/短债抵押品，分出来的钱主要是**抵押品的利息**
（所以水平贴着短端利率，跟 SGOV 一个量级），外加掉期调仓产生的**资本利得
分配**。跟标的公司派不派息基本无关。

**修法**：解析 IBKR 的 `DividendType` 列，把 Capital Gain / Return of
Capital 从股息率的分子里剔掉（现金仍计入税前与净额，因为钱确实到账了），
标的旁打「含资本利得」提示。METU 因此从 4.65% 降到 3.87%，那 22% 的差额
是资本利得分配。

**教训**：**「到账的现金」不等于「收益」**。基金会把已实现收益和你自己的
本金（return of capital）从同一条现金通道分回来，光看 Type=Dividends 会把
三种性质完全不同的钱算成一种。IBKR 已经在 `DividendType` 里分好类了 ——
数据源给了分类字段就别自己按金额瞎猜。

（顺带：这也影响报税，短期资本利得分配和合格股息的处理不一样。）
